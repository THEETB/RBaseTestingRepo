workflow-name: R-{RVersion}-build
---
layerName:
  Baselayername: {baselayername}
  Baselayertag: {baselayertag}
  Build_layer_name: {buildlayername}
  Build_layer_tag: {buildlayertag}
  build-required: {buildrequired}
  Dockerfile: {DockerfileType}
  Env:
    {ToolName}: {ToolVersion}
  Additional:
    {VariableName}: {VariableValue}
